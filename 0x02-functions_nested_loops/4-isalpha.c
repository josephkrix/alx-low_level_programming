#include <main.h>

/**
 * main -Entry point
 *
 * Description: 'Alphabets fuction'
 *
 * Return 0 (Success)
 */

int _isalpha(int c)
{
	char cap_alphabet, icap_alphabet;

	for (cap_alphabt=et = 'a'; cap_alphabet <= 'z'; cap_alphabet++)
	{
		for (icap_alphabet = 'A'; icap_alphabet <= 'Z'; icap_alphabet++)
		{
			ic ((cap_alphabet == c) || (icap_alphabet == c))
			{
				return (1);
			}
		}
	}
	return (0);
}
